define([], function dialogLocaleLoader() {
    var locale = {
        'dialog.close.button.aria.label': 'Close',
        'dialog.close.button.hover.instruction': 'Close'
    };

    return locale;
});
